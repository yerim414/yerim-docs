# CLAUDE.md

## 이 저장소

GitBook으로 발행하는 개인 기술 문서 저장소. Notion에서 마이그레이션했다.
문서는 전부 한글 마크다운이며 이미지는 `.gitbook/assets/`에 평평하게 모여 있다.

발행 설정은 [.gitbook.yaml](.gitbook.yaml)에 있고, 사이드바는 [SUMMARY.md](SUMMARY.md)가 결정한다.

GitBook 사용법 가이드는 `GITBOOK-GUIDE.md`에 있다. 로컬 참고용이라 커밋하지 않으므로
저장소에는 없을 수 있다.

## 작업 방식

**혼자 하는 프로젝트다. 브랜치를 따거나 PR을 만들지 않는다.**
`master`에서 직접 작업하고 바로 커밋·푸시한다.

Git Sync는 양방향이라 **사용자가 GitBook 웹 에디터에서 글을 쓰면 저장소에
커밋이 자동으로 생긴다.** 저장소 작업을 시작하기 전에 `git pull origin master`로
먼저 받아온다. 이걸 빼먹으면 충돌이 난다.

- 커밋/푸시는 사용자가 요청할 때만 한다.
- 브랜치 생성, PR 생성은 명시적으로 요청받지 않는 한 하지 않는다.
- 되돌리기 어려운 작업(force push, 히스토리 재작성 등)은 실행 전에 확인받는다.

## 사이드바 구조 규칙

한 메뉴의 직속 하위 항목이 **12개를 넘으면 주제별 그룹으로 묶는다.**
평평하게 늘어지면 스크롤 압박으로 탐색이 어려워진다.

GitBook의 SUMMARY 중첩은 **파일 시스템 구조와 무관하게 동작한다.**
그래서 그룹을 만들 때 문서 파일을 옮기지 않는다. 대신 그룹 인덱스 페이지만
`<섹션>/groups/` 아래에 두고 SUMMARY에서 한 단계 중첩시킨다.
이렇게 하면 기존 문서 URL이 바뀌지 않아 리다이렉트가 필요 없다.

예: `languages/python/groups/문법-기초.md`가 `languages/python/unpacking.md`를 하위로 갖는다.

## 문서를 추가·삭제할 때

- 새 문서는 [SUMMARY.md](SUMMARY.md)와 해당 섹션의 `README.md` **양쪽에** 링크해야
  사이드바에 노출된다. 한쪽만 넣으면 누락된다.
- 문서를 삭제하거나 병합하면 URL이 사라진다. [.gitbook.yaml](.gitbook.yaml)의
  `redirects`에 이전 경로 → 새 파일 경로를 추가한다.
- 정렬 순서는 라틴 문자 먼저, 그다음 한글이다.

## 작성 대기 중인 초안

내용이 비어 있는 문서는 SUMMARY에서 빼서 사이드바에 노출되지 않게 한다.
파일은 지우지 않고 상단에 "작성 중인 초안입니다" 안내를 남긴다.

**목록은 [DRAFTS.md](DRAFTS.md)에서 관리한다.** 이 저장소 작업을 할 때 한 번씩
확인하고, 채울 만한 것이 있으면 사용자에게 알린다. DRAFTS.md 자체는 SUMMARY에
등록하지 않으므로 GitBook에 발행되지 않는다.

## 마크다운 주의점

Notion에서 넘어온 제목에 `**`, `__` 같은 기호가 그대로 남아 있으면
마크다운이 서식으로 해석해 제목이 깨진다. 기호를 제목에 쓸 때는 백틱으로 감싼다.

예: `# __del__ vs __exit__` → 굵은 글씨로 렌더링됨. `` # `__del__` vs `__exit__` ``로 써야 한다.

## 발행

Git Sync가 동기화 브랜치(`master`)를 바라본다.
푸시하면 GitBook이 자동으로 가져가며 **별도 publish 작업은 필요 없다.**
`master`가 아닌 브랜치에 올린 변경은 사이트에 반영되지 않는다.

## 자격증명

과거 문서에 평문 비밀번호가 들어 있었고 히스토리 재작성으로 제거했다.
문서에 자격증명을 쓰지 않는다. 예시가 필요하면 `YOUR_PASSWORD` 같은
플레이스홀더를 쓴다. RFC1918 사설 IP(`192.168.x`)는 민감정보가 아니므로
그대로 두어도 된다.
