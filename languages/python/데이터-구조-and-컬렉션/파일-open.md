# 파일 open

파이썬에서 파일 여는거를 open 이라 했는데 기본이없다캄  f.open이라네

## **❤️ open()**

파이썬의 **내장 함수**로, 별도의 객체 없이 파일을 열 때 사용

```bash
file = open("test.txt", "r", encoding="utf-8")
content = file.read()
file.close()
```

f.open()? f = open인건가 뭘 말하는거지

```bash
f = open('workfile', 'w', encoding="utf-8")
```
